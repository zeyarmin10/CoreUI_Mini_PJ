import React from "react";
import { CCol, CRow, CTabs, CTabContent, CTabPane } from "@coreui/react";

const StudentRegExl = (props) => {
  return (
    <CTabContent>
      <CTabPane data-tab="regExcel"></CTabPane>
    </CTabContent>
  );
};

export default StudentRegExl;
